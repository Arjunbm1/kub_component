schemaVersion: 2.1.0
metadata:
    name: mydevfile_kub_comp
components:
- name: myk8deploy
    kubernetes:
      inlined: |
        apiVersion: batch/v1
        kind: Job
        metadata:
          name: pi
        spec:
          template:
            spec:
              containers:
              - name: job
                image: nginx
              restartPolicy: Never
